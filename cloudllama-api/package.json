{
  "name": "cloud-llama-api",
  "version": "0.4.0",
  "private": true,
  "description": "OAuth and playback service for the Cloud Llama Chrome extension",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "test": "node --test server.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
